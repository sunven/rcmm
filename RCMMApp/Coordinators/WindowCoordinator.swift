import Foundation
import SwiftUI
import RCMMShared
import os.log

/// 窗口协调器（暂未完成迁移）
///
/// 目前这些功能仍由 AppState 处理。
/// 这是一个占位符，用于未来完成迁移。
@Observable
@MainActor
final class WindowCoordinator {
    // 占位符：未来会实现完整的窗口管理功能
    // 目前由 AppState 继续负责
    
    init(forPreview: Bool = false) {
        // 空初始化
    }
}
